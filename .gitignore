.DS_Store

# API keys and local configuration
.env
.env.local
config.local.js
/tracks
build/
dist/
node_modules/

# IDE integration — generated locally, machine-specific paths, never committed.
# ufbt regenerates firmware/.vscode/compile_commands.json on every build;
# firmware/tests/compile_commands.json is produced by tests/gen_compile_commands.sh
compile_commands.json

# Flipper build artefacts (also covered by build/ and dist/ above)
*.fap
*.elf
