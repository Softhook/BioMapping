{
  "name": "visualiser",
  "private": true,
  "description": "Test harness for the browser-based GSR/RF track visualiser. No build step or bundler — the app itself stays plain <script>-tag globals; this only adds a Node test runner for tests/.",
  "scripts": {
    "test": "node --test tests/*.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
